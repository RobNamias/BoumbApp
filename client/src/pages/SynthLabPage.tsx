import React from 'react';
import SynthLab from '../components/organisms/SynthLab/SynthLab';

const SynthLabPage: React.FC = () => {
    return (
        <div style={{ height: '100%', width: '100%' }}>
            <SynthLab />
        </div>
    );
};

export default SynthLabPage;
